{
  "name": "seed_nodets_docker",
  "version": "1.0.0",
  "description": "Seed Template for Node(TS) project with Docker",
  "main": "dist/index.js",
  "scripts": {
	"dev": "nodemon src/index.ts",
	"build": "tsc -p .",
	"start": "NODE_PATH=./build node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "GatesKennedy",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
